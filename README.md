# ConcurrentHelloVideo

To compile:
cmake . && make && ./video

If dependencies are missing:
sudo apt-get install cmake libboost-thread-dev libboost-system-dev libx11-dev
cd /opt/vc/src/hello_pi
sudo ./rebuild.sh
